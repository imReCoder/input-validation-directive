import { VmNameValidatorDirective } from './vm-name.validator.directive';

describe('VmNameValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new VmNameValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
